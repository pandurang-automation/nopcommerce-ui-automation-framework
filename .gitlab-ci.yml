stages:
  - test

run_ui_tests:
  stage: test
  image: maven:3.9.6
  script:
    - mvn clean test -Denv=qa -Dheadless=true
  artifacts:
    when: always
    paths:
      - target/surefire-reports/
      - target/